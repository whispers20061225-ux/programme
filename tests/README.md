# Tests

Place automated tests in this directory.

Suggested convention:

- unit tests: `test_*.py`
- integration tests: `integration_*.py`

Hardware-dependent/manual checks should be placed in `scripts/` or `examples/`.
